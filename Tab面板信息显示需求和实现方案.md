# Tab 面板信息显示需求和技术方案

## 一、用户需求

### 1.1 用户要解决什么问题？

**核心痛点**：用户在多个 tab 之间切换时，无法快速识别每个 tab 的上下文信息。

**具体场景**：
- 用户打开了 5 个 tab，都在同一个项目目录下，但运行着不同的命令
- 用户切换到某个 tab，想快速知道：这个 tab 在哪个目录？在哪个 git 分支？在运行什么命令？
- 当前：用户需要切到 tab，然后看终端内容，或者运行 `pwd`、`git branch` 来确认
- 期望：tab 面板直接显示这些信息，一目了然

**价值**：
- 减少用户的认知负担
- 提高多 tab 场景下的工作效率
- 让 tab 面板成为"信息面板"，而不仅仅是"切换按钮"

---

### 1.2 用户要看什么信息？为什么要看？

Tab 面板需要显示以下信息：

#### 信息 1：标题

**为什么需要**：识别 tab 正在运行什么程序。

**例子**：
- 标题是 "claude" → 用户知道这个 tab 在运行 Claude
- 标题是 "vim" → 用户知道这个 tab 在编辑文件
- 标题是 "D:\work\wezteam" → 用户知道这个 tab 是一个 shell，当前在某个目录

**关键洞察**：标题可能是程序名，也可能是路径。用户需要区分这两种情况。

---

#### 信息 2：路径

**为什么需要**：知道 tab 的工作目录。

**场景**：
- 用户在 tab A 运行 `cd /project1`，在 tab B 运行 `cd /project2`
- 切换到 tab A 时，用户想快速确认：这个 tab 在哪个项目？
- 如果标题已经是路径（如 "D:\work\wezteam"），则不需要重复显示
- 如果标题是程序名（如 "claude"），则必须单独显示路径

**关键洞察**：路径必须以某种形式显示（标题或单独一行），否则用户无法识别 tab 的工作目录。

---

#### 信息 3：Git 分支

**为什么需要**：知道 tab 在哪个 git 分支上工作。

**场景**：
- 用户在 main 分支开发新功能，在 develop 分支修复 bug
- 切换到某个 tab 时，用户想快速确认：这个 tab 在哪个分支？
- 避免：在错误的分支上运行命令（如在 main 分支上运行了本应在 develop 分支运行的命令）

**关键洞察**：Git 分支是开发者最重要的上下文信息之一，必须显示。

---

#### 信息 4：当前命令

**为什么需要**：知道 tab 正在运行什么命令。

**场景**：
- 用户在 tab A 运行 `cargo build`，在 tab B 运行 `npm install`
- 切换到 tab A 时，用户想快速确认：这个 tab 在运行什么？还在编译吗？
- 如果命令已结束，则不显示（只显示 shell）

**关键洞察**：当前命令是动态信息，只在有非 shell 命令运行时才显示。

---

### 1.3 核心需求：开箱即显

**问题**：首次打开终端时，git 分支信息不显示。

**为什么这是个问题**：
- 用户打开终端，期望立即看到 git 分支
- 但当前实现：需要等待用户运行某个命令后，git 分支才会显示
- 这违背了"信息面板"的定位：信息应该立即可见，不需要用户触发

**根本原因**（技术层面）：
- Git 分支显示依赖路径
- 路径获取依赖 OSC 7 或进程检查
- 首次打开终端时，shell 还未运行，OSC 7 未发送
- 进程检查可能失败（Windows 权限问题）
- 结果：路径获取失败 → git 分支无法显示

---

### 1.4 核心需求：动态更新

**问题**：用户运行 `cd` 或切换 git 分支后，信息不立即更新。

**为什么这是个问题**：
- 用户运行 `cd ../project2`，期望 tab 面板立即显示新路径
- 用户运行 `git checkout develop`，期望 tab 面板立即显示新分支
- 但当前实现：需要等待下次渲染，可能有延迟

---

## 二、核心技术特点

### 2.1 数据获取的多层回退机制

**核心设计**：路径获取采用三层回退机制。

```
OSC 7（最准确）→ 进程检查（实时）→ initial_cwd（兜底）
```

#### 为什么需要三层回退？

**技术限制 1：OSC 7 的时序问题**

- OSC 7 是 shell 通过 precmd 钩子发送的
- 首次打开终端时，shell 还未运行，OSC 7 未发送
- 如果只依赖 OSC 7，首次打开时路径为空

**技术限制 2：进程检查的权限问题**

- Windows 上，读取进程的 CWD 需要权限
- 某些情况下，进程检查会失败（返回 None）
- 如果只依赖进程检查，权限不足时路径为空

**技术限制 3：进程检查的可靠性问题**

- Unix 上，进程检查依赖 `tcgetpgrp()` 获取前台进程组
- 某些情况下，`tcgetpgrp()` 返回 0（没有前台进程组）
- 如果只依赖进程检查，tcgetpgrp 失败时路径为空

**权衡取舍**：
- 单一数据源不可靠 → 采用多层回退
- OSC 7 最准确，但有时序问题 → 作为第一优先级
- 进程检查实时，但有权限问题 → 作为第二优先级
- initial_cwd 不实时，但绝对可靠 → 作为最终兜底

**为什么 initial_cwd 是最终兜底？**
- initial_cwd 是 pane 创建时的路径，由用户在命令行指定（如 `wezterm start --cwd /path`）
- 这个数据在创建时就确定，不会变化，绝对可靠
- 但它不是实时路径（shell 启动后可能 `cd` 到其他目录）
- 所以只在前两者都失败时使用

---

### 2.2 Git 分支读取：直接读取 .git/HEAD

**核心设计**：不执行 `git branch` 命令，直接读取 `.git/HEAD` 文件。

#### 为什么不执行 git 命令？

**技术限制 1：Windows 控制台窗口闪烁**

- 在 Windows 上执行 `git branch` 会短暂显示控制台窗口
- 用户会看到一个黑色窗口闪一下，体验很差
- 这是 Windows 的进程创建机制导致的，无法避免

**技术限制 2：命令执行的性能开销**

- 执行 `git branch` 需要启动 git 进程
- 进程启动有开销（Windows 上尤其慢）
- 每次渲染 tab 面板都执行命令，性能无法接受

**技术限制 3：命令执行的时序问题**

- 如果 tab 面板在渲染时执行命令，会阻塞渲染
- 用户会感觉到卡顿

**权衡取舍**：
- 执行 git 命令 → 体验差、性能差、有卡顿
- 直接读取文件 → 体验好、性能好、无卡顿
- 选择：直接读取 `.git/HEAD` 文件

#### 为什么直接读取 .git/HEAD 可行？

**技术分析**：
- `.git/HEAD` 文件存储当前分支引用
- 格式：`ref: refs/heads/main`（正常分支）或 commit hash（detached HEAD）
- 读取文件是原子操作，不会阻塞
- 文件内容就是分支信息，不需要解析 git 命令的输出

**实现细节**：
1. 向上查找 `.git` 目录（处理子目录情况）
2. 读取 `.git/HEAD` 文件
3. 如果以 `ref: refs/heads/` 开头，提取分支名
4. 否则，显示 "HEAD"（detached HEAD）

---

### 2.3 标题与路径的智能判断

**核心设计**：根据标题内容判断是否需要单独显示路径。

#### 为什么需要智能判断？

**用户场景**：
- 场景 1：标题是 "claude"（程序名）→ 需要单独显示路径
- 场景 2：标题是 "D:\work\wezteam"（路径）→ 不需要重复显示路径

**技术挑战**：如何判断标题是路径还是程序名？

**方案 1：检查标题是否是有效路径**

```rust
fn title_is_path(title: &str) -> bool {
    Path::new(title).exists()
}
```

**问题**：
- 标题是 "main"（程序名），但恰好存在 `./main` 目录 → 误判为路径
- 性能开销：每次渲染都要检查文件系统

**方案 2：检查标题是否包含路径分隔符**

```rust
fn title_is_path(title: &str) -> bool {
    title.contains('/') || title.contains('\\')
}
```

**问题**：
- 标题是 "git:main"（包含 `:`）→ 误判为路径（Windows 的 `C:` 格式）

**方案 3：综合判断（当前实现）**

```rust
fn title_is_path(title: &str) -> bool {
    // 包含路径分隔符
    title.contains('/') || title.contains('\\') ||
    // 或者是绝对路径（Windows 的 C: 格式）
    title.contains(':')
}
```

**权衡取舍**：
- 方案 1 准确，但性能差
- 方案 2 快速，但有误判
- 方案 3 平衡准确性和性能

**已知问题**：
- 标题是 "git:main" 会被误判为路径（包含 `:`）
- 但这种情况很少见，可以接受

---

### 2.4 缓存机制：平衡性能和实时性

**核心设计**：git 分支和当前命令使用缓存，缓存时长 1000ms。

#### 为什么需要缓存？

**技术限制**：
- 读取 `.git/HEAD` 需要文件 I/O
- 获取当前命令需要进程信息查询
- 每次渲染都执行这些操作，性能开销大
- Tab 面板可能每 16ms 渲染一次（60 FPS）

**权衡取舍**：
- 不缓存 → 性能差，CPU 占用高
- 永久缓存 → 信息不更新
- 短时缓存（1000ms）→ 平衡性能和实时性

#### 缓存键的设计

**当前实现**：`(pane_id, cwd)`

**问题**：
- 用户在同一个目录切换 git 分支
- 缓存键不变 → 显示旧的分支名（最多 1 秒）

**改进方案**：缓存键包含 `.git/HEAD` 的修改时间

```rust
let cache_key = (pane_id, cwd, git_head_mtime);
```

**权衡取舍**：
- 当前方案简单，但有短暂不一致
- 改进方案准确，但增加了文件 I/O（获取 mtime）
- 选择：先实现当前方案，后续优化

---

### 2.5 Shell 命令的过滤

**核心设计**：只显示非 shell 命令。

#### 为什么需要过滤？

**用户场景**：
- 用户在 shell 中，前台进程是 shell 本身
- 显示 "bash" 或 "zsh" 没有意义
- 只显示用户运行的命令（如 "cargo build"）

**技术实现**：

```rust
fn is_shell_command(cmd: &str) -> bool {
    let shells = ["bash", "zsh", "fish", "sh", "pwsh", "powershell", "cmd"];
    shells.iter().any(|&s| cmd.contains(s))
}
```

**权衡取舍**：
- 过滤 shell 命令 → 信息更有价值
- 不过滤 → 显示无意义的 "bash"、"zsh"

---

## 三、边界情况

### 3.1 首次打开终端

**场景**：用户打开终端，shell 还未运行。

**技术状态**：
- OSC 7 未发送
- 进程检查可能失败
- 路径为空

**解决方案**：使用 `initial_cwd` 兜底。

**为什么可行**：
- `initial_cwd` 是用户在命令行指定的路径（或当前目录）
- 这个数据在创建 pane 时就确定，绝对可靠
- 虽然不是实时路径，但首次打开时是准确的

---

### 3.2 用户运行 cd 命令

**场景**：用户运行 `cd /other/path`。

**技术状态**：
- Shell 通过 precmd 钩子发送 OSC 7
- OSC 7 包含新路径
- 触发 `CurrentWorkingDirectoryChanged` 事件

**更新机制**：
- OSC 7 立即更新 `terminal.current_dir`
- 事件触发重新渲染
- 重新计算路径和 git 分支

**延迟**：
- OSC 7：立即更新（0ms）
- Git 分支：最多 1000ms（缓存）

---

### 3.3 用户切换 git 分支

**场景**：用户运行 `git checkout develop`。

**技术状态**：
- `.git/HEAD` 文件内容改变
- 但缓存键 `(pane_id, cwd)` 不变

**问题**：
- 缓存未过期 → 显示旧的分支名 "main"
- 最多 1 秒后缓存过期 → 显示新的分支名 "develop"

**改进方案**：
- 缓存键包含 `.git/HEAD` 的修改时间
- 文件修改 → 缓存键改变 → 立即重新读取

---

### 3.4 进入/离开 git 仓库

**场景 1：进入 git 仓库**

- 用户运行 `cd project`（project 是 git 仓库）
- OSC 7 触发重新渲染
- `find_git_dir` 向上查找，找到 `.git` 目录
- 显示 git 分支

**场景 2：离开 git 仓库**

- 用户运行 `cd ..`
- OSC 7 触发重新渲染
- `find_git_dir` 向上查找，找不到 `.git` 目录
- 不显示 git 分支

---

### 3.5 程序修改标题

**场景**：用户运行 `vim`，vim 通过 OSC 0 设置标题为 "vim"。

**技术状态**：
- OSC 0 触发 `TabTitleChanged` 事件
- 标题从 "bash" 变为 "vim"
- 重新判断 `title_is_path`

**更新机制**：
- 事件触发重新渲染
- 重新判断是否需要显示路径行

---

### 3.6 SSH 会话

**场景**：用户通过 SSH 连接到远程主机。

**技术限制**：
- OSC 7 包含远程路径：`file://hostname/path`
- 当前实现只解析本地路径
- 远程路径无法显示

**权衡取舍**：
- 解析远程路径 → 需要区分本地和远程
- 当前方案：只显示本地路径
- 未来改进：支持远程路径显示

---

### 3.7 Windows 权限问题

**场景**：Windows 上，进程检查失败。

**技术限制**：
- 读取进程的 CWD 需要 `PROCESS_QUERY_INFORMATION` 权限
- 某些情况下，权限不足
- 进程检查返回 None

**解决方案**：
- OSC 7 通常可用（shell integration）
- 如果 OSC 7 也失败，使用 `initial_cwd` 兜底

---

## 四、技术限制总结

### 4.1 OSC 7 的限制

- **时序问题**：首次打开终端时，OSC 7 未发送
- **依赖 shell integration**：需要配置 shell 的 precmd 钩子
- **远程路径**：SSH 会话的 OSC 7 包含远程路径，当前无法解析

### 4.2 进程检查的限制

- **权限问题**：Windows 上需要 `PROCESS_QUERY_INFORMATION` 权限
- **可靠性问题**：Unix 上 `tcgetpgrp()` 可能返回 0
- **性能问题**：进程信息查询有开销

### 4.3 Git 分支读取的限制

- **依赖 .git 目录**：必须在 git 仓库中
- **detached HEAD**：无法显示分支名，只能显示 "HEAD"
- **缓存一致性**：切换分支时可能短暂显示旧分支

### 4.4 标题判断的限制

- **误判问题**："git:main" 会被误判为路径
- **性能权衡**：不检查文件系统，可能有误判

---

## 五、权衡取舍总结

### 5.1 数据获取：多层回退 vs 单一数据源

**选择**：多层回退（OSC 7 → 进程检查 → initial_cwd）

**理由**：
- 单一数据源不可靠（各有各的限制）
- 多层回退保证在所有限制下都能工作
- 代价：实现复杂度增加

### 5.2 Git 分支读取：文件读取 vs 命令执行

**选择**：文件读取

**理由**：
- 命令执行在 Windows 上有控制台窗口闪烁问题
- 文件读取性能更好
- 代价：无法获取 git 状态（未提交的更改）

### 5.3 缓存：短时缓存 vs 无缓存

**选择**：短时缓存（1000ms）

**理由**：
- 无缓存性能差
- 永久缓存信息不更新
- 短时缓存平衡性能和实时性
- 代价：切换分支时可能短暂显示旧分支

### 5.4 标题判断：启发式判断 vs 文件系统检查

**选择**：启发式判断（检查路径分隔符）

**理由**：
- 文件系统检查性能差
- 启发式判断快速
- 代价：少数情况误判（如 "git:main"）

---

## 六、实现方案

### 6.1 核心修改

**修改 1**：`LocalPane` 添加 `initial_cwd` 字段

- 文件：`mux/src/localpane.rs`
- 目的：存储 pane 创建时的初始路径
- 作为 OSC 7 和进程检查失败时的兜底

**修改 2**：`get_current_working_dir()` 添加回退逻辑

- 文件：`mux/src/localpane.rs`
- 逻辑：OSC 7 → 进程检查 → initial_cwd
- 保证始终能获取路径

**修改 3**：`spawn_pane()` 传递 `initial_cwd`

- 文件：`mux/src/domain.rs`
- 逻辑：将 `command_dir` 转换为 `PathBuf` 并传递

**修改 4**：`fancy_tab_bar.rs` 添加路径显示逻辑

- 文件：`wezterm-gui/src/termwindow/render/fancy_tab_bar.rs`
- 逻辑：根据 `title_is_path` 判断是否显示路径行

**修改 5**：添加 `simplify_path()` 函数

- 文件：`wezterm-gui/src/termwindow/render/fancy_tab_bar.rs`
- 目的：简化路径显示（如 `/home/user` → `~/`）

**修改 6**：SSH 和 tmux 添加 `None` 参数

- 文件：`mux/src/ssh.rs`、`mux/src/tmux_commands.rs`
- 原因：这些 pane 没有 `initial_cwd`

### 6.2 实现原则

**原则 1：最小修改**

- 只修改必要的代码
- 不重构现有逻辑
- 不影响其他功能

**原则 2：向后兼容**

- 不改变现有 API
- 新增字段使用 `Option` 类型
- 默认值为 `None`

**原则 3：优雅降级**

- 每个数据源都可能失败
- 失败时回退到下一个数据源
- 最终保证有数据可用

---

## 七、验证方案

### 7.1 功能验证

**测试 1**：首次打开终端

- 打开终端，立即检查 tab 面板
- 期望：显示路径和 git 分支

**测试 2**：运行 cd 命令

- 运行 `cd /other/path`
- 期望：路径立即更新

**测试 3**：切换 git 分支

- 运行 `git checkout develop`
- 期望：git 分支立即更新（最多 1 秒延迟）

**测试 4**：进入/离开 git 仓库

- 运行 `cd project`（git 仓库）
- 期望：显示 git 分支
- 运行 `cd ..`
- 期望：git 分支消失

### 7.2 边界情况验证

**测试 5**：标题是路径

- 运行 `wezterm start --cwd /path`
- 期望：标题显示路径，不重复显示路径行

**测试 6**：标题是程序名

- 运行 `vim`
- 期望：标题显示 "vim"，单独显示路径行

**测试 7**：SSH 会话

- 运行 `ssh user@host`
- 期望：标题显示 "ssh"，路径行可能为空（当前限制）

**测试 8**：Windows 权限不足

- 在权限受限的环境下打开终端
- 期望：使用 `initial_cwd` 兜底，显示路径

---

## 八、未来改进方向

### 8.1 Git 状态显示

**当前问题**：只显示分支名，不显示状态（未提交的更改）

**改进方案**：
- 读取 `.git/index` 和工作目录状态
- 显示 "✓"（干净）或 "✗"（有更改）

**权衡**：
- 需要扫描工作目录，性能开销大
- 可能需要异步执行

### 8.2 缓存一致性改进

**当前问题**：切换分支时可能短暂显示旧分支

**改进方案**：
- 缓存键包含 `.git/HEAD` 的修改时间
- 文件修改 → 缓存失效 → 立即重新读取

### 8.3 远程路径支持

**当前问题**：SSH 会话的路径无法显示

**改进方案**：
- 解析 OSC 7 中的 `file://hostname/path`
- 区分本地和远程路径
- 显示远程路径（可能需要简化显示）

### 8.4 标题判断改进

**当前问题**："git:main" 会被误判为路径

**改进方案**：
- 检查标题是否是有效路径（文件系统检查）
- 或者使用更精确的启发式规则

**权衡**：
- 文件系统检查性能差
- 可能接受当前的误判（少数情况）
