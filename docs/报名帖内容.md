【标题】【学习工作赛道】TeamShell - AI 多智能体协作终端工作台

【标签】学习工作

【正文】

**1. 创意名称 + 创意介绍**

* 想解决什么问题：当前 AI Agent 都是单兵作战，当任务复杂时容易陷入死循环或盲区，无法像人类团队那样分工协作、互相帮助、互相监督；同时单 Agent 处理复杂任务上下文消耗大，token 成本高但效率低。

* 为什么会想到做这个：受到 Chrome DevTools MCP 的启发——如果 AI 能操控浏览器，为什么不能操控终端？终端是开发者最重要的工具，让多个 AI Agent 像人类团队一样坐在终端前工作，就能实现真正的 AI 团队协作。

* 大概是什么产品：一个基于 Wezterm 终端的多 Agent 协作系统（Protocol + CLI + 终端集成），支持任意 Agent（Claude/Codex 等）和任意 shell（pwsh/cmd/zsh/bash/fish 等），让 AI 在终端标签页中工作、沟通、协作，团队会话可自动恢复。

**2. 目标用户及痛点**

* 面向哪些用户：需要 AI 辅助编程的开发者、需要自动化运维多步骤任务的工程师、需要 AI 协助做调研分析的知识工作者。

* 在什么场景下使用：复杂任务分解（把大任务交给多个 Agent 并行处理）、代码开发与审查、协作 debug（一个 Agent 卡住了，其他 Agent 来帮忙）、模拟团队工作流（产品/开发/测试分工协作）、多 Agent 混合协作（不同能力/成本的 Agent 协同工作）。

* 当前痛点：单 Agent 处理复杂任务时容易上下文耗尽、重复试错；没有好的人工介入机制，人类难以监管 AI 的执行过程；Agent 之间无法自然协作，无法像人类团队那样交接工作；不同 Agent 能力和成本不同，难以灵活调配；使用大模型全程处理所有任务，token 成本高但不一定高效。

**3. 价值与意义**

* 效率提升：将串行的单 Agent 任务变成并行的多 Agent 协作，任务完成时间可大幅缩短。更重要的是，主 Agent 只负责思考和监管，具体执行工作由低成本 Agent 完成，节省 token 的同时不失智能，综合效率可提升 3-5 倍。

* 社会价值：首次实现了 AI Agent 的团队工作模式，为未来 AI 团队协作提供了基础设施，可应用于软件开发自动化、运维自动化、研究助理等领域，开启 AI 协作新时代。同时系统不绑定任何特定模型或平台，支持任意 Agent 和任意 shell，具有良好的通用性和可扩展性。

**4. 必须附带：** [teamshell-proposal.html|attachment] (上传 HTML 文件)
